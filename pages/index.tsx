import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <h1 className="text-3xl font-bold underline">
      <button className="btn">dw</button>
    </h1>
  );
};

export default Home;
